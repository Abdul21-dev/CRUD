<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Qoura</h1>
    <% for(post of posts) {%>
    <div class="posts">
        <h3 class="user">@<%= post.username %></h3>
        <h3 class="content"><%= post.content %></h3>
        <a href="http://localhost:8080/posts/<%=post.id%>" class="newpost"> Show this post in detail</a> &nbsp;
    <a href="http://localhost:8080/posts/<%=post.id%>/edit" class="newpost" >Edit</a>
    <form method="post" action="/posts/<%=post.id%>/?_method=DELETE">
        <button>Delete</button>
    </form>
    </div>
    <% }%>
    <br/>
     <a href="http://localhost:8080/posts/new" class="newpost">Create New posts</a>
</body>
</html>